{
  "name": "@gamepark/game-template",
  "version": "0.0.0",
  "description": "The rules of Game Template adapted for Game Park",
  "sideEffects": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "Romain Fromi <romain@game-park.com> (https://game-park.com/)",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "vitest",
    "build": "tsc",
    "prepack": "yarn build",
    "publish": "yarn version ${0:-patch} && yarn npm publish ${OTP:+--otp $OTP} && git commit -am v$(node -p \"require('./package.json').version\") && git tag v$(node -p \"require('./package.json').version\") && git push --follow-tags",
    "lint": "eslint --fix"
  },
  "peerDependencies": {
    "@gamepark/rules-api": ">=7.7",
    "es-toolkit": "^1.45.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@gamepark/rules-api": "~7.7.2",
    "@typescript/native": "npm:typescript@^7.0.2",
    "es-toolkit": "^1.52.0",
    "eslint": "^10.9.1",
    "globals": "^17.12.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-eslint": "^8.69.0",
    "vitest": "^4.1.11"
  }
}
